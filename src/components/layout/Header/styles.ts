import styled from '@emotion/styled'

export const Content = styled.header`
  padding: 1rem 0;
  text-align: center;
`

export const LogoImage = styled.figure`
  position: relative;
  display: inline-block;
  width: 1.5rem;
  height: 1.5rem;
  vertical-align: middle;
`
export const LogoText = styled.figure`
  position: relative;
  display: inline-block;
  width: 7.5rem;
  height: 1.5rem;
  margin-top: 0.25rem;
  vertical-align: middle;
`
